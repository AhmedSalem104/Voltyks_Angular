// CapacityDto is already defined in lookup.model.ts
// Only define Create and Update DTOs here

export interface CreateCapacityDto {
  kw: number;
}

export interface UpdateCapacityDto {
  kw: number;
}
