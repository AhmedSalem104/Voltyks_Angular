{
  "buildCommand": "cd voltyks-admin && npm run build",
  "installCommand": "cd voltyks-admin && npm install",
  "outputDirectory": "voltyks-admin/dist/voltyks-admin/browser",
  "functions": {
    "api/**/*.js": {
      "runtime": "nodejs20.x"
    }
  },
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "/api/$1"
    },
    {
      "src": "/(.*)",
      "dest": "/$1"
    }
  ]
}
